# PI RP Engine

LLM 无关的角色扮演引擎。兼容酒馆（SillyTavern）角色卡格式。

## 使用说明

- `setup.bat` 一键安装依赖 + 编译
- `node scripts/import-card.mjs <角色卡>` 导入角色卡
- `.pi/cards` — 卡存放地址 | `.pi/presets` — 预设 | `.pi/sessions` — 历史

---

## 与酒馆（SillyTavern）的区别

| | 酒馆 | PI RP Engine |
|---|---|---|
| 定位 | 前端 UI 工具 | 后端引擎 |
| 模型绑定 | 需配置 API 格式（Kobold/OAI 等） | LLM 无关，通过 pi.dev 适配任意模型 |
| 上下文管理 | 手动调整 | 自动双预算调度 + 四种降级策略 |
| 世界书 | 关键词触发 | 关键词 + TF-IDF 语义匹配，且自动编译为 Skill |
| 变量/脚本 | regex + tavern 脚本 | 同格式兼容，导入时自动解析为结构化 schema |
| 前端 | 功能丰富的全 GUI | 轻量 Web 界面（可选） |
| 部署 | 桌面应用 | CLI / HTTP / WebSocket 三种模式 |
| Agent 集成 | 无 | 原生 pi.dev agent 工具链 |

---

## 当前好处

**1. 智能上下文调度。** target/hard 双阈值自动控制预算，超出时按 truncate → summarize → compress → drop 逐级降级，不用手动算 token。

**2. 世界书自动编译为 Skill。** 常开条目按内容归类为规则/风格/判定/变量等技能文件，触发条目保留关键词匹配，比酒馆的纯关键词触发更结构化。

**3. 与 pi.dev 深度集成。** 角色扮演过程中 AI 可直接调用 `read_state` / `update_state` / `search_worldbook` / `advance_time` 四个内置工具，状态追踪不依赖 prompt 拼接。

**4. 中文友好。** bigram TF-IDF 余弦相似度检索专为中文优化，比酒馆的英文分词器对中文世界书匹配更准确。
